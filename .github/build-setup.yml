# Injected into cargo-dist's build-local-artifacts job (Cargo.toml
# `github-build-setup`). gpq-proto's build script runs protoc, and the
# Protobuf Edition 2024 schemas require protoc 33+ (ADR 0004), which no
# GitHub runner image ships.
- name: Install protoc
  uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
  with:
    version: "35.1"
    repo-token: ${{ secrets.GITHUB_TOKEN }}
